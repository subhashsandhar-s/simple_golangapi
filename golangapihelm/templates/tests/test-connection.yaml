apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "golangapihelm.fullname" . }}-test-connection"
  labels:
    {{- include "golangapihelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "golangapihelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
