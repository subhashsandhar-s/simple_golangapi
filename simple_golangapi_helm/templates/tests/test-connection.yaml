apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple_golangapi_helm.fullname" . }}-test-connection"
  labels:
    {{- include "simple_golangapi_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple_golangapi_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
